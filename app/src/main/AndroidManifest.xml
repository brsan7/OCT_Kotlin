<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.brsan7.oct">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_oct"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_oct"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".CalendarioSolarActivity"></activity>
        <activity android:name=".RegistroLocalizacaoActivity" />
        <activity android:name=".LocalizacaoActivity" />
        <activity android:name=".RegistroEventoActivity" />
        <activity android:name=".CalendarioEventoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>